Setup Instructions:
Download all files.
Run "cd msm-app" in Terminal, followed by "node server.js".
Wait for the "Server running on port 3000" code and open "http://localhost:3000" to run the app.
Press the "Reset All" button if nothing appears.
